# [Project-Specific Guidelines]

This file is reserved for project-specific rules and context for AI agents.
Any instructions defined here will complement or override the global rules loaded from the `opencode.json` configuration.

- **Example Rule 1:** All API endpoints must be prefixed with `/api/v2/`.
- **Example Rule 2:** Use `pnpm` instead of `npm` for all package management.
